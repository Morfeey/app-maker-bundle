{% if uses.count() == 1 %}{{ [uses.first().getAlias() ? uses.first().getAlias() : uses.first().getClassName(), uses.first().isArray() ? '[]' : ''] | join }}{% else %}{% for keyUse, use in uses %}{% if uses.isLast(keyUse) %}{{ use.getAlias() ? use.getAlias() : use.getClassName() }}{{ use.isArray() ? '[]' : '' }}{% else %}{{ use.getAlias() ? use.getAlias() : use.getClassName() }}{{ use.isArray() ? '[]' : '' }}|{% endif %}{% endfor %}{% endif %}